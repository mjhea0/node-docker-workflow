machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - docker-compose up
    - cd app; mocha

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
