machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - docker-compose up web
    - cd app; mocha

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
