machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - docker-compose
    - cd app; mocha

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
